import { useLocation } from "react-router-dom";
import '../../../scss/view/comment.scss';

export default function BoardComment() {

    return (
        <div className="comment_area">
            댓글영역
        </div>
    );
}
